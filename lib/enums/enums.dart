
//To check if the authentication is processing -Busy or the authentication is -Idle
enum ViewState  { Idle, Busy}

//To check if user is Patient or Doctor
enum UserType { Patient, Doctor, Unknown }


//HomePage views

enum TabItem { home, appointment, chat, search, person }
